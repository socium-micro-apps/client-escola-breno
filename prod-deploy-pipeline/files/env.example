# =============================================================================
# .env — __SLUG__
# =============================================================================
# Este arquivo vai para /opt/socium-sf/clients/__SLUG__/.env na VPS.
# NUNCA commitar valores reais — apenas o exemplo neste pacote.
#
# IMAGE_TAG é atualizado automaticamente pelo CI a cada deploy.

IMAGE_TAG=latest

# Adicione abaixo as variáveis específicas da sua app. Exemplos:
#
# POSTGRES_PASSWORD=GERAR_SENHA_FORTE_AQUI
# SECRET_KEY=GERAR_SECRET_FORTE_AQUI
# STRIPE_SECRET=
# EMAIL_API_KEY=
